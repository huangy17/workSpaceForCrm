<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bjpowernode.crm.workbench.dao.ClueActivityRelationDao">

    <delete id="unbund">

        delete from tbl_clue_activity_relation where activityId=#{id}

    </delete>


	<select id="getActivityListByClueId" resultType="Activity">

        select
            a.id,
            u.name as owner,
            a.name,
            a.startDate,
            a.endDate,
            a.cost,
            a.description,
            a.createTime,
            a.createBy,
            a.editTime,
            a.editBy
        from tbl_clue_activity_relation car join tbl_activity a on car.activityId = a.id join tbl_user u on u.id = a.owner

        where car.clueId = #{clueId}

    </select>
  	
</mapper>

































